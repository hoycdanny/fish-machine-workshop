apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-session-service
  namespace: fish-game-system
  labels:
    app: game-session-service
    project: fish-machine-workshop
    workshop: fish-machine-workshop
    managed-by: 3.eks-service-deployment-deploy.sh
spec:
  replicas: 1
  selector:
    matchLabels:
      app: game-session-service
  template:
    metadata:
      labels:
        app: game-session-service
    spec:
      containers:
      - name: game-session-service
        # 注意：需要替換為你的 ECR 映像 URI
        image: <AWS_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/fish-game-session
        ports:
        - containerPort: 8082
        envFrom:
        - configMapRef:
            name: fish-game-config
        env:
        - name: SERVICE_PORT
          value: "8082"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 5